[DONE] Phase 1 — Models: Category, Tag, Product, ProductImage, ProductVariant, Review, Order, OrderItem, UserProfile, Wishlist
[DONE] Phase 1 — Migrations applied for products, orders, users
[DONE] Phase 1 — Session cart (cart.py), settings wired up
[DONE] Phase 2 — All frontend templates: home, shop, collection, product_detail, search, cart, checkout, order_confirm, login, register, dashboard
[DONE] Phase 2 — Components: announcement_bar, navbar (mega menu), footer, newsletter_popup, product_card
[DONE] Phase 2 — HTMX cart: add/update/remove, cart drawer, cart count
[DONE] Phase 2 — Wishlist HTMX toggle (users/partials/wishlist_btn.jinja) — was missing, now fixed
[DONE] Phase 3 — control/ app created with full Mission Control CMS at /admin/
[DONE] Phase 3 — AdminLog model + migration applied (control/migrations/0001_initial.py)
[DONE] Phase 3 — control/services.py: dashboard stats, ban/unban, order status update, log_action
[DONE] Phase 3 — control/forms.py: ProductForm, CategoryForm, ProductImageForm, OrderStatusForm
[DONE] Phase 3 — control/views.py: Dashboard, Stats (HTMX), Users (HTMX search), UserDetail, Ban/Unban, Products (HTMX search), ProductCreate/Edit/Delete, ImageUpload/Delete, Categories CRUD, Orders (status filter), OrderDetail
[DONE] Phase 3 — control/urls.py: all routes under /admin/ namespace=control
[DONE] Phase 3 — Templates: base_control, dashboard, users, user_detail, products, product_edit, categories, category_edit, orders, order_detail + all partials
[DONE] Phase 3 — Added control to INSTALLED_APPS, wired path('admin/', include('control.urls')) in config/urls.py
[DONE] Home page — replaced "Shop by Shape" with 6-category image grid; removed ring creator CTA section
[DONE] /sd admin — full control: ProductImage, ProductVariant, enhanced Product/Order/User/Review admins, AdminLog registered read-only
[DONE] /admin control panel — Payment + Refund models added, migrated; full Shopify-standard features built:
  - Customers page (buyers list + lifetime value + detail with stats/orders/wishlist)
  - Payments page (list + filter by status + order link)
  - Refund Tracker (list + manage status + create from order detail)
  - Inventory page (stock levels, HTMX live toggle active/featured/new_arrival, inline stock update)
  - Reviews page (approve/reject HTMX toggle, delete)
  - Order detail expanded (payment form, refund create/manage, timeline)
  - Dashboard: 12 live stats (revenue today/month/total, low stock, refunds pending, failed payments)
  - Sidebar: Inventory, Reviews, Payments, Refunds, Customers added
[DONE] Fix /sd/orders/order/add/ TypeError — OrderItem.total_price handles None price/qty on empty admin inlines
[DONE] OrderItemInline — editable line items + safe line_total display; order numbers use MX prefix
[DONE] Missing cart/partials/cart_count.jinja for HTMX add-to-cart badge update
[DONE] Cart context processor + navbar live cart count
[DONE] About + Contact pages (templates/core/about.jinja, contact.jinja)
[DONE] Collection 404 fix — style slugs (solitaire-rings, halo-rings, etc.) resolve via products/services.py keyword filters + slug aliases
[DONE] Full project audit — cart ghost-item prune, drawer remove HTMX, navbar cart count OOB updates
[DONE] Wishlist unauth → HX-Redirect login; login next URL validated; cart template product checks fixed
[DONE] End-to-end flow verified: storefront, collections, cart, checkout, /sd admin, /admin control (37 checks)
[DONE] Live 404 fix — production DB had 0 products/categories; resolve_collection now
  auto-creates core catalog categories; seed_data uses slug-based get_or_create
[IN PROGRESS] Phase 4 — Payment integration, email notifications, SEO
[DONE] Full audit pass — bug/security/perf fixes:
  - Fixed overselling: Cart.add() clamps to Product.stock; checkout now runs
    select_for_update + re-validates stock + decrements it inside one atomic
    transaction (previously stock was never checked or deducted anywhere)
  - cart_add/cart_update no longer 500 on non-numeric quantity (cart/services.py
    parse_quantity helper, clamped 1-999)
  - Wired existing python-dotenv dep into config/settings.py: SECRET_KEY/DEBUG/
    ALLOWED_HOSTS now read from .env with safe dev fallbacks; added .env.example
  - Perf: Cart no longer prunes invalid products (DB query) on every single
    page load via the global context processor — only in views that actually
    show/mutate cart contents (cart detail/add/update/remove/drawer, checkout)
  - Perf: Product.primary_image was doing 1-2 fresh queries per product per
    render (N+1 across shop/collection/search/home product grids); rewritten
    to read from prefetch cache, and prefetch_related('images') added to all
    product list querysets (core, products, control)
  - Verified via `manage.py check`, dev server smoke test, and shell-level
    Cart stock-clamp + parse_quantity checks — no regressions found
[DONE] Deploy readiness pass:
  - core/models.py: SiteSettings singleton (logo, email, phone, whatsapp,
    address, business hours, social links) editable at /sd/core/sitesettings/
    — one row enforced, list view redirects straight to the edit form.
    Migration core/migrations/0001_initial.py generated, NOT yet applied —
    awaiting user confirmation to run `manage.py migrate` (safety rule)
  - core/context_processors.py: site_settings on every Jinja2 page; falls
    back to settings.py SITE_BRAND/SITE_EMAIL if the table isn't migrated
    yet (caught OperationalError/ProgrammingError — was 500ing every page
    until this fallback was added, since the processor fires pre-migration)
  - Wired footer/navbar/base title/contact page to site_settings instead of
    hardcoded strings; logo swaps in automatically once uploaded
  - config/settings.py: CACHES + SESSION_ENGINE use django-redis only when
    DEBUG=False (locmem + DB sessions in dev, no redis server required
    locally); DATABASES switches to Postgres automatically when POSTGRES_DB
    env var is set, else sqlite; SECURE_*/HSTS/SSL-redirect/CSRF_TRUSTED_ORIGINS
    all gated behind DEBUG=False; whitenoise wired for static
  - requirements.txt: + gunicorn, whitenoise, django-redis, psycopg2-binary
  - Dockerfile, entrypoint.sh, docker-compose.yml (web/db/redis/nginx),
    nginx/nginx.conf, .dockerignore, DEPLOY.md (full VPS deploy runbook —
    migrate is a manual step by design, not run from entrypoint.sh)
  - .env.example expanded with Postgres/Redis/CSRF_TRUSTED_ORIGINS keys

NEXT STEP (needs user confirmation): run `python manage.py migrate core` to
create the SiteSettings table — site works fine without it (falls back to
settings.py values) but /sd/core/sitesettings/ won't actually save anything
until it's applied.

[DONE] Mission Control roles (Superuser + Store Manager):
  - UserProfile.role (customer | store_manager) + users/migrations/0002
    (applied locally; run migrate on prod when ready)
  - control/permissions.py: can_access_control, SuperuserRequiredMixin,
    login URL /admin/login/
  - Mission Control login/logout (Jinja dark theme); customers redirected
    to /accounts/dashboard/
  - Role-aware sidebar (Users/Activity/Settings = superuser only) + badge
  - Superuser: assign role, ban, impersonate, activity log
  - Store Manager: catalog/orders/payments/inventory/reviews/customers view
  - Customer portal: order detail, profile edit, MC link for staff
  - Seeded manager@mnxstore.com / Manager@1234 — see test_user.txt

